{% comment %}
  Lumina Pro - Dynamic Group (Universal Container)
  Architecture: Theme Blocks 2024 Standard
  此 Section 是全站最通用的布局容器，支持在后台任意插入原子 Blocks。
{% endcomment %}

{{ 'component-base.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .dynamic-group-{{ section.id }} {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: {{ section.settings.content_alignment }};
    text-align: {{ section.settings.content_alignment }};
    max-width: {{ section.settings.container_width }}rem;
    margin: 0 auto;
    gap: {{ section.settings.block_spacing }}rem;
  }

  /* 针对 Flex 对齐的修复 */
  .dynamic-group-{{ section.id }} > * {
    width: 100%;
  }
{%- endstyle -%}

<div class="color-{{ section.settings.color_scheme }} gradient section-{{ section.id }}-padding">
  <div class="page-width">
    <div class="dynamic-group-{{ section.id }} container-vibe-{{ section.settings.visual_vibe }}">
      
      {%- comment -%} 
        核心渲染指令：
        它会拉取 templates/*.json 中该 section 节点下定义的所有 Theme Blocks。
      {%- endcomment -%}
      {% content_for 'blocks' %}
      
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Dynamic Group",
  "tag": "section",
  "class": "section-dynamic-group",
  "blocks": [
    {
      "type": "@theme"
    }
  ],
  "settings": [
    {
      "type": "header",
      "content": "Layout (布局)"
    },
    {
      "type": "select",
      "id": "container_width",
      "label": "Container max width",
      "options": [
        { "value": "60", "label": "Narrow (600px)" },
        { "value": "80", "label": "Standard (800px)" },
        { "value": "100", "label": "Wide (1000px)" },
        { "value": "140", "label": "Full Page" }
      ],
      "default": "80"
    },
    {
      "type": "select",
      "id": "content_alignment",
      "label": "Content alignment",
      "options": [
        { "value": "flex-start", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "flex-end", "label": "Right" }
      ],
      "default": "center"
    },
    {
      "type": "range",
      "id": "block_spacing",
      "min": 1,
      "max": 5,
      "step": 0.5,
      "unit": "rem",
      "label": "Spacing between blocks",
      "default": 2
    },
    {
      "type": "header",
      "content": "Style (风格)"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "visual_vibe",
      "label": "Visual Vibe",
      "info": "为容器添加特殊的视觉质感",
      "options": [
        { "value": "none", "label": "Default" },
        { "value": "glass", "label": "Glassmorphism (毛玻璃)" },
        { "value": "soft-shadow", "label": "Soft Floating (柔和阴影)" }
      ],
      "default": "none"
    },
    {
      "type": "header",
      "content": "Spacing (间距)"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 60
    }
  ],
  "presets": [
    {
      "name": "Dynamic Group",
      "blocks": []
    }
  ]
}
{% endschema %}